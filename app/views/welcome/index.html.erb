<div class="container">
  <div class="row">
    <% for i in 1..12 do %>
      <div class="col-sm-4 clear">
        <h3> <%= link_to Wiki.find(i).title, Wiki.find(i) %> </h3>
        <p> <%= Wiki.find(i).body %></p>
      </div>
    <% end %>
  </div>
</div>
